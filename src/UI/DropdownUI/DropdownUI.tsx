import React from "react";

export const DropdownUI = () => {
  return (
    <h1>
      use: <a href="https://react-select.com/home">React Select</a>
    </h1>
  );
};

export default React.memo(DropdownUI);
